services:
  - type: web
    name: narrativescope-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: CHROMA_PERSIST_DIR
        value: /data/chroma_db
