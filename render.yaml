services:
  - type: web
    name: narrativescope-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: .
    envVars:
      - key: CHROMA_PERSIST_DIR
        value: /data/chroma_db
